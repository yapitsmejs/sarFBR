# Normalize line endings: LF in the repo, native on checkout. Pairs with
# .editorconfig and ruff's `line-ending = "lf"` so editors, the formatter, and
# git all agree.
* text=auto eol=lf

# Binary files -- never normalize.
*.png binary
*.jpg binary
*.ico binary
*.zip binary